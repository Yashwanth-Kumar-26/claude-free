#!/usr/bin/env bash
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Use .venv python if available (uv sync creates it), otherwise system python
if [ -x "$DIR/.venv/bin/python3" ]; then
    PYTHON="$DIR/.venv/bin/python3"
else
    PYTHON="python3"
fi

cd "$DIR"
exec "$PYTHON" -m cli.entrypoints "$@"
